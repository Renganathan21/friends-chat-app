{
  "version": 2,
  "builds": [
    { "src": "index.js", "use": "@vercel/node" }  // Ensure 'index.js' is your entry point
  ],
  "routes": [
    { "src": "/(.*)", "dest": "index.js" }  // Route all requests to your entry point
  ]
}
